"""Compatibility marker for the rejected pre-staging real-view draft.

The executable DEV-001 implementation is dev_real_staged_view. This module
intentionally exposes no exporter so validation targets cannot share a
pre-prediction view with model inputs.
"""

SUPERSEDED_BY = "quant_stack_v2.dev_real_staged_view"
