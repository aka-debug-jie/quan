"""Compatibility marker for the rejected pre-staging real-runner draft.

The executable DEV-001 implementation is dev_real_staged_runner, which freezes
predictions before validation targets are opened.
"""

SUPERSEDED_BY = "quant_stack_v2.dev_real_staged_runner"
